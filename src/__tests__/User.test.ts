
describe("Users",() => {

  it("should be able to pass test", () => {
    const number = 0;

    expect(number).toBe(0);
  });

  it("should be able to pass the second test", () => {
    const number = 1;

    expect(number).toBe(1);
  });

});
